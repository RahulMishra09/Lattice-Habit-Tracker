services:
  - type: web
    name: lattice
    runtime: node
    rootDir: backend
    buildCommand: npm install
    startCommand: node server.js
    envVars:
      - key: MONGODB_URI
        sync: false   # set manually in Render dashboard
      - key: DB_NAME
        value: lattice
      - key: PORT
        value: 3001
